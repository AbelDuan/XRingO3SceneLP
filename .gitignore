# ---- 打包 / 编辑器产物 ----
*.zip
*.tgz
*.bak
*.bak-*
*.orig
*.rej
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# ---- 自检脚本的沙盒（tools/test_sync_skip.sh 会在这里建假 Scene 目录）----
.test_sync_skip/
dist/

# ---- 本地采集到的设备数据（可能含个人包名/截图，不入库）----
*.png
*.apk
*.db
*.xml
scratch/
_scratch_*/

# ---- 自检脚本在模块目录内建的临时沙盒（见 tools/test_*.py 的 mkdtmp）----
#   ⚠ 故意**不**在测试退出时 rmtree：宿主 safe-delete 钩子会因「批量删除 >50 文件」
#     抛 SystemExit(1)，把全绿测试的退出码染成 1。残留目录交给本规则忽略。
_t_*/
_t_*

# ---- 测试/调试报告（本地产物，不入库）----
_rep_*.txt
_test_all_report.txt
_t[0-9]*.log
_probe*.txt
