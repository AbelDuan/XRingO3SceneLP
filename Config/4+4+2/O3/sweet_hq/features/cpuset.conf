# SceneO3Tuner · cpuset（处理器亲和）—— 玄戒 O3
#
# ⚠⚠ 本文件的 5 个键（use_presets / in_apps / in_games / ebpf_enhanced / perf_notify）
#    **在 Scene 的 classes.dex / resources.arsc 里都搜不到**（2026-09-18 逐键核对）。
#    高度怀疑同样是无效键 —— 但 APK 里存在 perf_cpuset_top / perf_cpuset_foreground /
#    cpuset_bg / cpuset_top_app 等 id，**对应关系未确认** → 按项目铁律「不猜」，
#    这里**保持原键名不动**，仅加此警告；等能确认真实键名后再改。
#    影响：若确实无效，则本文件从未生效，实际行为由 Scene 自身默认值决定。
use_presets=0
in_apps=0
in_games=0
ebpf_enhanced=0
perf_notify=1
